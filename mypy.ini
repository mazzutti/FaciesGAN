[mypy]
# Exclude the virtualenv and build directories from mypy checks
exclude = (?:^|/)(\.venv|build|dist|__pycache__)(?:/|$)
# Don't follow imports into third-party packages
ignore_missing_imports = True
